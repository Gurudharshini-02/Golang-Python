def numPalindrome(n):
    temp=n
    res=0
    while n>0:
        lastDigit=n%10
        res=res*10+dig
        n=n/10
    if temp==res:
        print("Palindrome")
    else:
        print("Not a palindrome")
a=int(input("Enter a word: "))
numPalindrome(a)
